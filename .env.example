# Local application; all values are consumed. Process environment overrides this file.
VITE_APP_TITLE=my-project
HOST=127.0.0.1
PORT=18080
WEB_HOST=127.0.0.1
VITE_PORT=2711
LOG_LEVEL=info
# Optional. Relative paths resolve from the project root; omit for the platform application-data directory.
# APP_DATA_DIR=.runtime
